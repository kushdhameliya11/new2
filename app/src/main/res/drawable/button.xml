<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled state -->
    <item android:drawable="@color/disable" android:state_enabled="false" />
    <!-- Enabled state -->
    <item android:drawable="@color/black" />
</selector>
